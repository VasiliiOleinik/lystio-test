export enum BUTTON_TYPES {
    button = 'button',
    submit = 'submit',
    reset = 'reset'
}

export enum DEBOUNCE_DELAY {
    short = 300,
    medium = 500,
    long = 700,
    extraLong = 1000
  }